from litestar import Router

from {{ importname }}.api.routes.root.controller import Controller as RootController

router = Router(
    path="/",
    route_handlers=[
        RootController,
    ],
)
