from litestar import Router

from {{ importname }}.api.routes.ping.router import router as ping_router
{%- if events and sse %}
from {{ importname }}.api.routes.sse.router import router as sse_router
{%- endif %}

router = Router(
    path="/",
    route_handlers=[
        ping_router,
        {%- if events and sse %}
        sse_router,
        {%- endif %}
    ],
)
